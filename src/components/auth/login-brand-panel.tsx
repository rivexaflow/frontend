/** Right brand panel — original dot grid + subtle blue/purple glow pulse (no bottom rise). */
export function LoginBrandPanel() {
  return <aside className="login-brand-panel hidden lg:flex" aria-hidden />;
}
