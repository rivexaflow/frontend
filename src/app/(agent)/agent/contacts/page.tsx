export default function AgentContactsPage() {
  return (
    <section className="rounded-xl border border-[var(--rvx-midnight)]/10 bg-[var(--rvx-white)] p-4">
      <h1 className="text-xl font-semibold">Assigned Contacts</h1>
      <p className="mt-2 text-sm text-[var(--rvx-midnight)]/70">Only contacts assigned to logged-in agent are visible.</p>
    </section>
  );
}
