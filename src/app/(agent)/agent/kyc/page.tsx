export default function AgentKycPage() {
  return (
    <section className="rounded-xl border border-[var(--rvx-midnight)]/10 bg-[var(--rvx-white)] p-4">
      <h1 className="text-xl font-semibold">My KYC Uploads</h1>
      <p className="mt-2 text-sm text-[var(--rvx-midnight)]/70">Upload documents and track verification status.</p>
    </section>
  );
}
