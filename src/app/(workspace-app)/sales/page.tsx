import { SalesView } from "@/features/workspace/views/sales-view";

export const metadata = {
  title: "Sales & Client Quotations — RivexaFlow",
  description: "Formulate target proposals, pipeline quotations, and complete customer orders",
};

export default function SalesPage() {
  return <SalesView />;
}
