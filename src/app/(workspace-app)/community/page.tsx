import { CommunityView } from "@/features/workspace/views/community-view";

export const metadata = {
  title: "Workspace Community Board — RivexaFlow",
  description: "Share discussions, collaborate on internal topics, and post Q&As",
};

export default function CommunityPage() {
  return <CommunityView />;
}
