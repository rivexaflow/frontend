import { SlaView } from "@/features/workspace/views/sla-view";

export const metadata = {
  title: "SLA Support Policies — RivexaFlow",
  description: "Configure support response targets, milestones warning triggers, and escalation pathways",
};

export default function SlaPage() {
  return <SlaView />;
}
