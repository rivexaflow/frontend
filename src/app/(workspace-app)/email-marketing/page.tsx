import { EmailMarketingView } from "@/features/workspace/views/email-marketing-view";

export const metadata = {
  title: "Email Marketing — RivexaFlow",
  description: "Send automated campaigns, newsletters and template blocks",
};

export default function EmailMarketingPage() {
  return <EmailMarketingView />;
}
