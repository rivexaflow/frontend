import { PosView } from "@/features/workspace/views/pos-view";

export const metadata = {
  title: "POS Checkout Terminal — RivexaFlow",
  description: "Point of Sale retail counter, barcode processing, cart checkouts and logs invoices",
};

export default function PosPage() {
  return <PosView />;
}
