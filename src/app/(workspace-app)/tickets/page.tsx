import { TicketsView } from "@/features/workspace/views/tickets-view";

export const metadata = {
  title: "Support Tickets — RivexaFlow",
  description: "Create support queries, monitor resolution progress, and live chat with account representatives",
};

export default function TicketsPage() {
  return <TicketsView />;
}
